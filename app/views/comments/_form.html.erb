<%= form_for [@article, @comment] do |f| %>
  <%= f.text_field :comment, class: "comment-input", placeholder:"Write a comment"%>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <%= f.submit "Submit", class:"comment-submit"%>
<% end %>